@using School.BLL.DTO
@model UserDTO

@{
    ViewData["Title"] = "Teacher Profile";
}

<div class="flex-column align-items-center">
    <h4>Teacher profile</h4>
    <div class="col-md-6">
        @if (TempData["Error"] != null)
        {
            <div class="col-md-12 alert alert-danger">
                <span><b>Sorry!</b> - @TempData["Error"]</span>
            </div>
        }
        @if (Model.FirstName == null && Model.LastName == null)
        {
            <h4>Please create your profile</h4>
            <a class="btn btn-outline-success float right" asp-controller="Profile" asp-action="CreateProfile">Create</a>
        }
        else
        {
            <form method="post" asp-action="TeacherProfile" enctype="multipart/form-data">
                <input type="hidden" name="Id" value="@Model.Id" />
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="form-group">
                    <label asp-for="FirstName" class="control-label"></label>
                    <input asp-for="FirstName" class="form-control">
                    <span asp-validation-for="FirstName" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="LastName" class="control-label"></label>
                    <input asp-for="LastName" class="form-control">
                    <span asp-validation-for="LastName" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="DateOfBirth" class="control-label"></label>
                    <input asp-for="DateOfBirth" class="form-control" type="date" />
                    <span asp-validation-for="DateOfBirth" class="text-danger"></span>
                </div>
                <p> </p>
                <div class="form-group">
                    <input class="btn btn-outline-success float-right" type="submit" value="Save" />
                </div>
                <p> </p>
            </form>

           @*  @if (Model.Castings != null)
            {
                <div class="row row-cols-auto row-cols-md-auto g-4">
                    @foreach (var casting in Model.Castings)
                    {
                        <div class="col">
                            <div class="card shadow-sm h-100">
                                <div class="card-body">
                                    <p class="card-text fw-bold">@casting.Title</p>
                                    <p class="card-text fw-bold">@casting.Role</p>
                                    <p class="card-text">@casting.Description</p>
                                    <p class="card-text">@casting.StartDate.Date - @casting.EndDate.Date</p>
                                    <p class="card-text">@casting.Location</p>
                                    <div class="d-flex justify-content-between align-items-center">
                                        <div class="btn-group">
                                            <a asp-controller="Casting" asp-action="DetailForDirector" asp-route-castingId="@casting.Id"
                                               type="button" class="btn btn-sm btn-outline-secondary">Detail</a>
                                            @if (User.Identity.IsAuthenticated && User.IsInRole("Director"))
                                            {
                                                <a asp-controller="Casting" asp-action="Edit" asp-route-castingId="@casting.Id"
                                                   type="button" class="btn btn-sm btn-outline-secondary">Edit</a>
                                                <a asp-controller="Casting" asp-action="Delete" asp-route-castingId="@casting.Id"
                                                   onclick="return confirm('Are you sure?')" type="button" class="btn btn-sm btn-outline-secondary">Delete</a>
                                            }
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    }
                </div>
            } *@
        }
    </div>
</div>
